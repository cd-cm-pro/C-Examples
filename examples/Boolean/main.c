#include <stdio.h>
#include <stdbool.h> // bool required

int main(void) {
    bool a = true; // bool variable
    if (a) {
        printf("a is true\n");
    } else {
        printf("a is false\n");
    }
    return 0;
}